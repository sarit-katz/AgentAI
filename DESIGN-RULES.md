# בסיס ידע AI — הוראות לעריכה

אתר סטטי בעברית RTL שמתעד את הידע של תפקיד Chief of AI לצורך חפיפה, ומדגים את
היכולות שהוא מתאר. אין framework, אין build step, אין npm.

הקובץ הזה הוא **חוקים בלבד** — מה מותר ומה אסור בעריכה. תיאור המבנה, ההרצה
ותוספת נושא נמצאים ב-[README.md](README.md).

## אילוצים שאין לשבור

- **האתר חייב לעבוד מ-`file://`.** לכן **אסור `fetch()`**, אסור ES modules בדפדפן,
  ואסור כל דבר שדורש server. דאטה נטענת כ-`<script>` שמגדיר `window.SITE` /
  `window.SEARCH_INDEX`.
- **אפס תלויות.** לא להוסיף ספריות, לא CDN של JS, לא CSS framework. הדבר החיצוני
  היחיד הוא Google Fonts, ויש לו fallback מלא — האתר קריא גם בלי אינטרנט.
- **JavaScript של הדפדפן ב-ES5.** `var`, `function`, בלי arrow functions ובלי
  optional chaining, כדי שהקובץ ירוץ גם בדפדפן ארגוני נעול.
  Node (`tools/*.mjs`) הוא היחיד שכותבים בו ES modules מודרני.
- **קובץ CSS אחד** (`assets/styles.css`), מחולק לסעיפים ממוספרים עם כותרת בעברית.
  סעיף חדש נכנס במקומו הלוגי, והמספור מתוקן כדי להישאר רציף.

## מערכת העיצוב

- **קודם טוקנים, אחר כך קוד.** כל צבע, ריווח, רדיוס, פונט ומשך אנימציה יושבים
  כמשתנה CSS בראש `styles.css`. **אין לכתוב ערך גלם** בשום מקום אחר —
  `color: #8b5cf6` בגוף הקובץ הוא באג, גם אם הוא נראה נכון.
- **בהיר היא ברירת המחדל** (`data-theme="light"` על `<html>` בכל 13 העמודים);
  מצב כהה הוא ערכה חמה שלמה, לא היפוך. **שתי הערכות חייבות לעמוד בניגודיות
  4.5:1 לטקסט.** `app.js` בוחר כהה רק אם נשמר במפורש ב-`localStorage` או אם
  המערכת מוגדרת כהה.
- **השפה החזותית נגזרה מ-`spiritt.ai`** (הטוקנים נשלפו מהאתר החי, לא מהזיכרון):
  קנבס שמנת (`--bg: #F9F6EF`), משטחים לבנים (`--s-1` / `--s-2`)
  עם צללים רחבים ורכים, אקסנט סגול (`--brand: #7B16AF`, `--violet-2: #9333EA`),
  pill כמעט־שחור ל-CTA (`--pill`), ומסגרת לילך (`--lilac`) לפאנל הסוגר.
  רדיוסים 10 / 16 / 20 / 26 / 999 — קופסאות מעוגלות, לא מרובעות.
- **טיפוגרפיה:** `--font-disp` (Josefin Sans) לכותרות ולספרות הדקות של `.num`,
  `--font-sans` (Nunito) לגוף, `--font-mono` (Geist Mono) למונו.
  לשלושתם **אין גליפים עבריים** — Rubik מופיע אחריהם בשרשרת ה-fallback ותופס
  את העברית לכל אורך האתר. **אין להסיר את Rubik מאף font stack.**
- **בלוק ה-aliases** בסוף סעיף הטוקנים (`--forest`, `--brass`, `--warn`,
  `--font-display` ועוד) נשאר. `demos.js` קורא לו inline; מחיקה שלו שוברת גרפים.
- **בלי אימוג'י כאייקון.** אייקון = SVG inline. אימוג'י בגוף הטקסט אסור גם כן.
- כל אלמנט לחיץ מקבל `cursor: pointer` ומצב `:focus-visible` נראה.
- `prefers-reduced-motion: reduce` מכובד — כל ה-transitions מתבטלים.
- ריספונסיביות נבדקת ב-**375 / 768 / 1024 / 1440**. הבדיקה היא
  `document.documentElement.scrollWidth === clientWidth` בכל אחד מהם.

## RTL — המלכודות שכבר נשרפנו בהן

- **תכונות לוגיות בלבד:** `margin-inline-start`, `padding-inline-end`,
  `border-inline-start`, `inset-inline-start`, `inline-size`.
  `left` / `right` / `margin-left` הם באג.
- **רדיוסים:** `border-radius: 0 14px 14px 0` הוא פיזי ונשבר. משתמשים ב-
  `border-start-start-radius`, `border-start-end-radius`,
  `border-end-start-radius`, `border-end-end-radius`.
- **גרדיאנטים הם היוצא מן הכלל:** `linear-gradient(to inline-end, …)` **אינו נתמך
  בדפדפנים** וכל ההצהרה נזרקת. ל"מפס האקסנט פנימה" ב-RTL כותבים
  `linear-gradient(270deg, …)`, עם הערה בקוד שמסבירה למה.
- **כל מקטע לטיני מבודד:** מונח, קוד, מספר גרסה, כתובת — מקבלים
  `dir="ltr"` ו/או `unicode-bidi: isolate`. בלי זה הפיסוק עף לצד הלא נכון.
- **לא לשים עברית בתוך אלמנט `direction: ltr`.** בפועל: `.code__label` הוא LTR,
  ולכן תווית קוד בעברית אסורה שם. למיקרו-תוויות בעברית יש `.label` ו-`.label--dim`.
- **גריד:** עמודות הן `minmax(0, 1fr)` ולא `1fr`. ילד רחב (טבלה עם
  `min-inline-size`) יגרום ל-overflow אופקי של כל העמוד אם כותבים `1fr`.
- `text-align: start` ולא `right`. בתוך אלמנט `dir="ltr"` הוא יפתור לשמאל — זה
  נכון, ולפעמים מפתיע.

## חוקי תוכן — הכי חשוב בקובץ הזה

האתר הזה מתעד ידע שמישהו יקבל החלטות לפיו. לכן:

1. **אפס המצאות.** אין לכתוב עובדה, מספר, גרסה או שם רכיב שלא הגיע מהחומר שהובא
   או מקריאה בפועל במקור.
2. **אין כתובות מנוחשות.** כתובת ש-לא נפתחה ואומתה נשארת `url: ''`, והפריט מופיע
   בעמוד המקורות תחת "קישורים להשלמה". כתובת GitHub שנראית נכונה ואינה קיימת היא
   הנזק הגרוע ביותר כאן.
3. **כל מספר שאינו מדידה שלנו מקבל `<span class="claim">`** עם המקור:
   `דיווח של Google · לא מדידה עצמאית`.
4. **אין מחירים בדולרים.** לא מהזיכרון, בשום מקום. מחשבון הקונטקסט עובד ביחידות
   יחסיות של "טוקנים שנקראו", והמסקנה שלו נשמרת בלי אף מספר דולרי.
5. **מה שחסר נכתב.** בלוק `<div class="note note--gap">` עם `מידע חסר`,
   ובו שתי שורות: **מה שאין** ו-**מה צריך להשלים** עם השאלה המדויקת.
6. **מצב אמיתי, לא שאיפה.** לכל כלי יש אחד משלושה מצבים, והוא נכתב בגלוי:
   `רץ כאן` · `בהערכה` · `לא הורץ אצלנו`.

## טון

גוף שני, בפנייה בלשון נקבה (המשתמשת היא Chief of AI). פסקאות קצרות.
בלי שפה שיווקית, בלי סופרלטיבים, בלי "פשוט" ו"רק". החלקים הלא נוחים נאמרים
במפורש: מה שביר, מה יקר, מה עוד לא נבדק. מונחים טכניים נשארים באנגלית בתוך
המשפט העברי.

## איך מוסיפים תוכן

- **נושא חדש:** להעתיק עמוד נושא קיים כתבנית (`08-memory-graphs.html` הוא הנקי
  ביותר), ולהוסיף **רשומה אחת** ל-`SITE.topics` ב-`assets/site-data.js`.
  מבנה העמוד קבוע: `why` → `terms` → `how` → (הדגמה) → `do` → `traps` →
  `decide` → `faq` → `sources` → `מידע חסר`. ה-`id` על כל `h2` מחייב — ה-TOC
  והאינדקס נבנים ממנו.
- **מונח במילון:** רשומה ב-`SITE.glossary`. העמוד והחיפוש מתעדכנים לבד.
- **מקור:** רשומה ב-`SITE.sources`. `url` ריק = עובר לבד לקבוצת הפערים.
- **רכיב הדגמה:** פונקציה חדשה ב-`assets/demos.js`, שיוצאת מיד אם ה-host שלה
  לא קיים בעמוד, ורישום שלה ב-`ready()` בסוף הקובץ.
- **אחרי כל עריכת תוכן:** `node tools/build-index.mjs`.

## אימות לפני סיום — לא להצהיר, לבדוק

אין לכתוב "העמוד תקין" בלי לפתוח דפדפן. הבדיקה המינימלית:

1. `node tools/build-index.mjs --check` עובר.
2. פתיחת העמוד ב-Chrome אמיתי: `console` נקי לגמרי.
3. ארבעה רוחבי מסך, ובכל אחד `scrollWidth === clientWidth`.
4. מצב בהיר ומצב כהה — טקסט קריא בשניהם.
5. ההדגמות: להזיז פקד בפועל ולראות שהפלט משתנה.
6. כל קישור פנימי חדש — לוודא שה-`id` שהוא מפנה אליו אכן קיים בעמוד היעד.
